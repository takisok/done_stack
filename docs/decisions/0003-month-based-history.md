# ADR 0003: 活動ログは月単位を基本にする

## Status

Accepted

## Context

活動ログページには、年間の草表示、日単位の記録、月単位の傾向が混在していました。
クリックで日付だけが変わると、画面全体の意味が揃わず落ち着かない表示になっていました。

## Decision

活動ログページは月単位を基本にします。
年間の草表示は、対象月を選ぶための月ナビとして扱います。

## Consequences

- ページ全体の文脈が月単位に揃います。
- 年間の草表示の情報量は維持できます。
- 草のクリックは、日ではなく月の選択として扱います。
